describe('flow of execution', function(){
  it('should exec all matching functions');
  it('should pass on to the subsequent matched callback');
  it('should call the original function with the modified arguments');
  it('should not call stop exec flow if an exception is thrown');
});
